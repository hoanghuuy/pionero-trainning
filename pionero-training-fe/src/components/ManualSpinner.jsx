import { Spinner } from "react-bootstrap";

export default function ManualSpinner() {
    return (
        <div className="w-100 text-center">
            <Spinner animation="border" />
        </div>
    );
}
